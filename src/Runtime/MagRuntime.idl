namespace Magpie.Runtime
{
    [default_interface]
    runtimeclass MagRuntime 
    {
        MagRuntime();

        void Run(UInt64 hwndSrc, MagSettings settings);
        void ToggleOverlay();
        void Stop();

        UInt64 HwndSrc { get; };

        Boolean IsRunning { get; };
        event Windows.Foundation.EventHandler<Boolean> IsRunningChanged;
    }
}
