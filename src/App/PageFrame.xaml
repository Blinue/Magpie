<UserControl
	x:Class="Magpie.PageFrame"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Magpie"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid Margin="{StaticResource PageHeaderMargin}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition MaxWidth="1000" Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock x:Name="TitleTextBlock" Grid.Row="0" Text="{x:Bind Title, Mode=TwoWay}" Visibility="Collapsed" Margin="0,0,0,10" FontSize="30" FontWeight="Bold" />

		<ScrollViewer Grid.Row="1" Padding="0,0,20,0" VerticalScrollBarVisibility="Auto">
			<ContentControl VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="0,0,0,10">
				<ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{x:Bind MainContent, Mode=TwoWay}" />
			</ContentControl>
		</ScrollViewer>
	</Grid>
</UserControl>
