<Page
	x:Class="Magpie.App.SettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Magpie.App"
	xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:Magpie.App.Controls"
	mc:Ignorable="d">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition MaxWidth="1000" Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="设置" Style="{StaticResource PageHeaderTextStyle}" />

		<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,40,0">
			<StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
				<controls:SettingsGroup MyHeader="外观和行为">
					<controls:Setting MyHeader="主题" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
						<controls:Setting.Icon>
							<FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xEF1F;"/>
						</controls:Setting.Icon>
						<controls:Setting.ActionContent>
							<ComboBox x:Name="ThemeComboBox" Width="150" SelectionChanged="ThemeComboBox_SelectionChanged" DropDownOpened="ComboBox_DropDownOpened">
								<x:String>浅色</x:String>
								<x:String>深色</x:String>
								<x:String>使用系统设置</x:String>
							</ComboBox>
						</controls:Setting.ActionContent>
					</controls:Setting>
					<controls:Setting MyHeader="便携模式" Description="更改用户数据的保存位置" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
						<controls:Setting.Icon>
							<FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xEC0A;"/>
						</controls:Setting.Icon>
						<controls:Setting.ActionContent>
							<ToggleSwitch x:Name="PortableModeToggleSwitch" Toggled="PortableModeToggleSwitch_Toggled" />
						</controls:Setting.ActionContent>
					</controls:Setting>
				</controls:SettingsGroup>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>
