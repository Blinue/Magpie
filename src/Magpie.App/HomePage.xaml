<Page
	x:Class="Magpie.App.HomePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Magpie.App"
	xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition MaxWidth="1000" Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="Magpie" Style="{StaticResource PageHeaderTextStyle}" />

		<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,40,0">
			<local:Dummy>
				<StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,10">
					<local:SettingsGroup MyHeader="激活">
						<local:SettingItem MyHeader="快捷键" Description="自定义进入和退出全屏的快捷键" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
							<local:SettingItem.Icon>
								<FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xEDA7;"/>
							</local:SettingItem.Icon>
							<local:SettingItem.ActionContent>

							</local:SettingItem.ActionContent>
						</local:SettingItem>
						<local:SettingItem MyHeader="计时器" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left">
							<local:SettingItem.Icon>
								<FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE916;"/>
							</local:SettingItem.Icon>
							<local:SettingItem.ActionContent>
								<Button Content="5s 后缩放" />
							</local:SettingItem.ActionContent>
						</local:SettingItem>
					</local:SettingsGroup>
				</StackPanel>
			</local:Dummy>
		</ScrollViewer>
	</Grid>
</Page>
